using Spark.Library.Database;

namespace dotnetbase.Application.Models;

public static class ChargeAmountType
{
    public const string FIXED = nameof(FIXED);
    public const string PERCENTAGE = nameof(PERCENTAGE);

}