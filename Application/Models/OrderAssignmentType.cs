using Spark.Library.Database;

namespace dotnetbase.Application.Models;

public static class OrderAssignmentType
{
    public const string PROVIDER = nameof(PROVIDER);
    public const string PARTNER = nameof(PARTNER);
}